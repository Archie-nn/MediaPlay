# 簡易音訊播放器(MediaPlay)1133334楊恩奇

這是一個輕量級的桌面應用程式，專門用於播放、循環播放及停止 WAV 格式的音訊檔案。透過此專案，可以學習如何使用 .NET 內建的 System.Media.SoundPlayer 類別進行多媒體操作。

## 主要功能
- 檔案瀏覽器：整合 OpenFileDialog，自動過濾並選取 .wav 檔案。

- 單次播放：載入指定路徑之音訊並執行單次播放。

- 循環播放 (Looping)：支援音效無間斷循環，適合背景音樂播放。

- 停止播放：透過檔案串流 (FileStream) 機制安全停止當前音效。

- 關閉防護：內建 FormClosing 確認視窗，防止意外關閉程式。

## 技術實作細節
本程式展示了 SoundPlayer 類別三種不同的初始化與操作方式：

1. 指定路徑播放
使用 SoundLocation 屬性指定檔案位置，並呼叫 Load() 確保資料加載後播放。

2. 循環播放
直接在建構子傳入路徑，並使用 PlayLooping() 開啟循環模式。

3. 檔案串流操作
透過 FileStream 開啟檔案，展示了如何結合串流物件與播放器來停止音訊。

## 介面配置說明 (UI Design)

| 控制項 | ID功能描述 |
|-----------|-----------------|
| button1 | 選取檔案：開啟對話框選取 WAV 檔並顯示路徑 | 
| button2 | 播放：執行單次音訊播放 |
| button3 | 循環播放：重複播放直到手動停止 | 
| button4 | 停止：關閉目前播放的音效 | 
| button5 | 結束程式：離開應用程式 | 
| txtPath | 顯示目前選取的音訊檔案完整路徑 | 

## 如何使用
執行 MediaPlay.exe。

點擊 「選取檔案」 按鈕，從電腦中挑選一個 .wav 格式的音效檔。

點擊 「播放」 或 「循環播放」 開始聆聽。

若要更換檔案或停止，點擊 「停止」。

## Screenshots

![screenshot](./MediaPlay.png)

點擊右上方關閉圖示或「結束程式」按鈕，並在彈出的對話框中確認。

## 注意事項
- 格式限制：System.Media.SoundPlayer 僅支援 WAV 格式，不支援 MP3 或其他壓縮格式。

- 路徑檢查：若選取的路徑包含無效字元或檔案已毀損，程式可能會拋出例外，建議確保檔案路徑正確。

